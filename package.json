{
  "name": "cc-plugin-shuorenhua",
  "version": "0.1.1",
  "private": true,
  "type": "module",
  "description": "Claude Code plugin that translates the current session transcript into a concise Chinese handoff.",
  "license": "MIT",
  "engines": {
    "node": ">=18.18.0"
  },
  "scripts": {
    "test": "node --test tests/*.test.mjs"
  }
}
